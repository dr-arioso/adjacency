# adjacency
A Python framework for building structured annotation, review, and elicitation workflows with humans and language models. Adjacency gives research and eval teams reusable protocols, participant backends, and session orchestration on top of TurnTurnTurn’s evented mesh. Provenance is first-class: the canonical record is the event stream.
